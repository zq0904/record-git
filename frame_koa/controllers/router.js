// var fn_xxx = async (ctx, next) => {
    // ctx.request.body.email // 获取post请求的数据对象
    // ctx.body = `<p></p>`; // ctx.response.body 响应内容发送给浏览器
    // ctx.render('index.html', {title: '标题'}) // 因为集成nunjucks 直接发送给浏览器
    // ctx.params.name 来获取路由中查询字符串
    // ctx.response.redirect('/'); // 重定向
// }

// module.exports = {
//     'GET /a': fn_xxx,
//     'POST /b/:name': fn_xxx
// }